"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.program = void 0;
const cli_1 = require("./cli");
Object.defineProperty(exports, "program", { enumerable: true, get: function () { return cli_1.program; } });
const parsers_1 = require("./parsers/parsers");
const analyzer_1 = require("./core/analyzer");
const html_1 = require("./reporters/html");
const notifications_1 = require("./reporters/notifications");
// For API usage
const reporter = {
    generate: async (options) => {
        const parsers = [
            new parsers_1.JestParser(),
            new parsers_1.PlaywrightParser(),
            new parsers_1.CypressParser(),
            new parsers_1.VitestParser(),
            new parsers_1.MochaParser()
        ];
        let parser = parsers.find(p => p.canParse(options.results));
        if (options.framework) {
            parser = parsers.find(p => p.name === options.framework);
        }
        if (!parser)
            throw new Error('Could not detect framework');
        let report = parser.parse(options.results);
        report = await analyzer_1.Analyzer.analyze(report);
        if (options.outputPath) {
            html_1.HtmlReporter.generate(report, options.outputPath);
        }
        if (options.notifications) {
            for (const config of options.notifications) {
                await notifications_1.NotificationService.send(report, config);
            }
        }
        return report;
    },
    analyze: async (report) => await analyzer_1.Analyzer.analyze(report)
};
exports.default = reporter;
