import { program } from './cli';
declare const reporter: {
    generate: (options: {
        results: any;
        framework?: "jest" | "playwright" | "cypress" | "vitest" | "mocha";
        outputPath?: string;
        notifications?: {
            type: "slack" | "teams";
            webhookUrl: string;
        }[];
    }) => Promise<import("./core/types").FinalReport>;
    analyze: (report: any) => Promise<import("./core/types").FinalReport>;
};
export default reporter;
export { program };
