import { FinalReport } from './types';
export declare class Analyzer {
    static analyze(report: FinalReport): Promise<FinalReport>;
    private static summarizeWithAI;
    private static clusterErrors;
    private static generateInsights;
}
