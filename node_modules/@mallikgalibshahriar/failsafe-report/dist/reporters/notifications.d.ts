import { FinalReport } from '../core/types';
export interface NotificationConfig {
    type: 'slack' | 'teams' | 'discord';
    webhookUrl: string;
}
export declare class NotificationService {
    static send(report: FinalReport, config: NotificationConfig): Promise<void>;
    private static sendToSlack;
    private static sendToTeams;
}
